servers:
  from:
    name: <server name>
    namespace: <namespace>
    object: <openshift object type [svc, pod, bc, dc, secret, etc]>
    only: <regular expresion list, can be null>
    ignore: <regular expresion list, can be null>
  to:
    name: <server name>
    namespace: <namespace>